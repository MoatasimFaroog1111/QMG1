[build]
builder = "railpack"

[deploy]
startCommand = "uvicorn qmg1.api.app:app --app-dir src --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
