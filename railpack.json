{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "uvicorn qmg1.api.app:app --app-dir src --host 0.0.0.0 --port $PORT --workers ${WEB_CONCURRENCY:-2} --timeout-keep-alive 5 --limit-concurrency ${UVICORN_LIMIT_CONCURRENCY:-100} --backlog 128"
  }
}
